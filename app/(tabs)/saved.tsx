import React from 'react'
import { Text, View } from 'react-native'

const saved = () => {
  return (
    <View> //View is a container component in react native
      <Text>saved</Text>
    </View>
  )
}

export default saved